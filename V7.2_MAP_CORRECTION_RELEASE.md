# Le Château des Langues — v7.2 Map Correction

## Changes
- Removed the map ambience system and its settings, storage keys and audio asset.
- Restored the complete pre-existing navigation drawer and profile/avatar tools.
- Cropped the supplied map artwork to the actual city area so the header, controls and compass are not duplicated.
- Restored the original map interaction model, zoom, pan, reset, search, classic view, manual save and guide.
- Added direct profile access from the top avatar and the Profil area on the map.
- Enabled the hospital route.
- Corrected welcome-text progression to use sentence boundaries with a browser fallback.
- Preserved the final welcome text for ten seconds before restoring the original button.

## Build status
`npm run build` could not be executed because the environment could not install the missing Next.js package. `npm install` failed at the package gateway, and the available node_modules directory was incomplete.
