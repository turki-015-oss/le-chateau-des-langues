# Le Château des Langues v7.1 — Premium Interactive Map

## Implemented
- Adopted the supplied high-resolution map image at `public/maps/le-chateau-v71-map.jpeg`.
- Rebuilt `/kingdom` with percentage-based transparent hotspots for all map locations and profile.
- Added a single premium entrance confirmation card and prevented double-dialog navigation.
- Preserved zoom, reset, menu, save, avatar, and profile controls.
- Added persistent map ambience controls (on/off, 0–100 volume, default reset) using localStorage.
- Added fade-in/fade-out, single audio instance, navigation stop, and speech ducking events.
- Added a project-owned generated ambience file at `public/audio/map-ambience.wav`.
- Added automatic gently animated compass.
- Added avatar selection, device upload, preview, and saved persistence.
- Enhanced the landing welcome button with synchronized progressive text and a 10-second final display.
- Added and linked the hospital route and entrance.
- Corrected affected building-page links to return to `/kingdom` instead of the landing screen.

## Build status
Dependency installation could not complete in the execution environment because the configured npm registry returned HTTP 503. Therefore `npm run build` was not falsely marked successful.
