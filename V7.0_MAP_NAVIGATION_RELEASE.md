# Le Château v7.0 — Map Navigation Fix

- Main entry button now opens `/kingdom` (the interactive map).
- Castle halls top back arrow now opens `/kingdom`.
- Bottom navigation label changed from `الرئيسية` to `الخريطة`.
- Bottom map button now opens `/kingdom`.
- Castle interior retains the French luxury visual style using a clean background without embedded text.
- Hall titles, descriptions, icons and controls remain real interactive UI elements.
